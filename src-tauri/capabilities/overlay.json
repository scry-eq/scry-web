{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "The floating game overlay window. Capabilities are per-window, so this is what keeps the overlay's leaner surface from being the main window's.",
  "windows": [
    "overlay"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
